---
title: "Variables de guilde"
description: "35 variables de guilde prises en charge par Rumi."
icon: "brackets-curly"
---

<Info>
Le contexte compte. Une variable renvoie `N/A` lorsque la commande ou l'événement en cours ne fournit pas sa valeur source. Ajouter `|fallback text` avant l'accolade fermante lorsque vous avez besoin d'une solution de repli plus conviviale.
</Info>


| Variable | Ce qu'il retourne | Exemple de sortie | Disponible en |
| --- | --- | --- | --- |
| <code>&#123;guild.afk_channel&#125;</code> | Afk Channel pour la guilde. | <code>Exemple de canal afk de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.afk_timeout&#125;</code> | Afk Timeout pour la guilde. | <code>Exemple de délai d'attente de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.banner&#125;</code> | URL de l’image de bannière. Source : guilde. | <code>Exemple de bannière de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.boost_count&#125;</code> | Nombre actuel de boosts du serveur. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.boost_tier&#125;</code> | Niveau de boost du serveur actuel. | <code>Niveau 2</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.category_channels&#125;</code> | Canaux de catégorie pour la guilde. | <code>Exemples de canaux de catégories de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.category_channels_count&#125;</code> | Nombre de canaux de catégorie pour la guilde. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.channels&#125;</code> | Canaux pour guilde. | <code>Exemples de canaux de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.channels_count&#125;</code> | Nombre de canaux pour la guilde. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.count&#125;</code> | Nombre actuel de membres. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.created_at&#125;</code> | Discord-heure de création formatée. Source : guilde. | <code>&lt;t:1781460000:F&gt;</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.created_at_timestamp&#125;</code> | Horodatage Unix pour l’heure de création. Source : guilde. | <code>&lt;t:1781460000:F&gt;</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.discovery&#125;</code> | Découverte pour guilde. | <code>Exemple de découverte de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.emoji_count&#125;</code> | Nombre d'emojis personnalisés sur le serveur. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.icon&#125;</code> | URL de l’image de l’icône. Source : guilde. | <code>Exemple d'icône de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.id&#125;</code> | Discord ID du serveur actuel. | <code>123456789012345678</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.key_features&#125;</code> | Principales fonctionnalités de la guilde. | <code>Exemples de fonctionnalités clés de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.max_members&#125;</code> | Nombre maximum de membres pour la guilde. | <code>Exemple de membres maximum de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.max_presences&#125;</code> | Présences maximales pour la guilde. | <code>Exemple de présences maximales de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.max_video_channel_users&#125;</code> | Nombre maximum d'utilisateurs de chaînes vidéo pour la guilde. | <code>Exemples d'utilisateurs de canaux vidéo de guilde Max</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.member_count&#125;</code> | Nombre actuel de membres. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.name&#125;</code> | Nom du serveur actuel. | <code>Développement de Rumi</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.owner_id&#125;</code> | Identifiant du propriétaire de la guilde. | <code>Exemple d'identifiant de propriétaire de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.preferred_locale&#125;</code> | Paramètres régionaux préférés pour la guilde. | <code>Exemple de paramètres régionaux préférés par la guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.region&#125;</code> | Région pour guilde. | <code>Exemple de région de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.role_count&#125;</code> | Nombre de rôles sur le serveur. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.shard&#125;</code> | Éclat pour guilde. | <code>Exemple de fragment de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.splash&#125;</code> | Splash pour la guilde. | <code>Exemple de splash de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.text_channels&#125;</code> | Canaux de texte pour la guilde. | <code>Exemples de canaux de texte de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.text_channels_count&#125;</code> | Nombre de canaux de texte pour la guilde. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.vanity&#125;</code> | Vanité pour guilde. | <code>Exemple de vanité de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.voice_channels&#125;</code> | Canaux vocaux pour la guilde. | <code>Exemples de canaux vocaux de guilde</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;guild.voice_channels_count&#125;</code> | Nombre de canaux vocaux pour la guilde. | <code>128</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;server.id&#125;</code> | Discord IDENTIFIANT. Source : serveur. | <code>123456789012345678</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
| <code>&#123;server.name&#125;</code> | Nom du serveur actuel. | <code>Développement de Rumi</code> | Intégrations, commandes personnalisées, répondeurs automatiques, appels de messages |
