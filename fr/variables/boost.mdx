---
title: "Boostez les variables"
description: "6 variables de boost prises en charge par Rumi."
icon: "brackets-curly"
---

<Info>
Le contexte compte. Une variable renvoie `N/A` lorsque la commande ou l'événement en cours ne fournit pas sa valeur source. Ajouter `|fallback text` avant l'accolade fermante lorsque vous avez besoin d'une solution de repli plus conviviale.
</Info>


| Variable | Ce qu'il retourne | Exemple de sortie | Disponible en |
| --- | --- | --- | --- |
| <code>&#123;boost.count&#125;</code> | Valeur de comptage. Source : boost. | <code>128</code> | Boostez les messages, invoquez des messages |
| <code>&#123;boost.next_tier&#125;</code> | Niveau suivant pour un boost. | <code>Exemple d'augmentation du niveau suivant</code> | Boostez les messages, invoquez des messages |
| <code>&#123;boost.perks&#125;</code> | Avantages pour le boost. | <code>Exemples d'avantages boostés</code> | Boostez les messages, invoquez des messages |
| <code>&#123;boost.tier&#125;</code> | Niveau pour le boost. | <code>Exemple de niveau de boost</code> | Boostez les messages, invoquez des messages |
| <code>&#123;boost.user_since&#125;</code> | Utilisateur depuis pour boost. | <code>Exemple de boost d'utilisateur depuis</code> | Boostez les messages, invoquez des messages |
| <code>&#123;boost.user_since_timestamp&#125;</code> | Horodatage Unix pour l'utilisateur depuis. | <code>1781460000</code> | Boostez les messages, invoquez des messages |
