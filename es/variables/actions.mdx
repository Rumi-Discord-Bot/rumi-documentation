---
title: "Variables de acciones"
description: "30 variables de acciones apoyadas por Rumi."
icon: "brackets-curly"
---

<Info>
Estas filas son plantillas de sintaxis, no ejemplos listos para copiar. Reemplace cada `<required>` campo, omitir o reemplazar `[optional]` campos y mantener los argumentos de acción directa en el orden mostrado. Campos de botones con clave, como `label=`, `style=`, y `action=` puede ser reorganizado.
</Info>

Para conocer las reglas de argumentos completas, el encadenamiento de componentes, el diseño desplegable y las comprobaciones de permisos, lea [Acciones de comandos personalizados](../custom-commands/actions).


| Sintaxis exacta | que hace | como leerlo | Disponible en |
| --- | --- | --- | --- |
| <code>$v&#123;button: label=&lt;text&gt; &amp;&amp; style=&lt;style&gt; &amp;&amp; action=&lt;action-spec&gt; &amp;&amp; [reply=&lt;text&gt;]&#125;</code> | Crea un botón que ejecuta una acción. Los campos clave se pueden reorganizar. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>$v&#123;button: label=&lt;text&gt; &amp;&amp; url=&lt;https-url&gt; &amp;&amp; [emoji=&lt;emoji&gt;]&#125;</code> | Crea un botón de enlace. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>allow_user=&lt;id&gt; &amp;&amp; allow_role=&lt;id&gt; &amp;&amp; allow_channel=&lt;id&gt;</code> | Restringir quién o dónde se puede ejecutar un componente. Cada campo también acepta ID separados por comas. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>action=&lt;action-spec&gt; &amp;&amp; action=&lt;action-spec&gt;</code> | Ejecute las acciones de los componentes de izquierda a derecha. Repita la acción = para cada paso. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>await=true &amp;&amp; once=true &amp;&amp; defer=true &amp;&amp; expires=&lt;duration&gt;</code> | Controle la repetición, el aplazamiento y el vencimiento. Estos campos clave se pueden reorganizar. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>page=&lt;next&#124;previous&#124;number&gt;</code> | Mover una respuesta interactiva a otro $v&#123;página&#125;. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>reply=&lt;text&gt;</code> | Envíe una respuesta de componente efímera. Usar público=&lt;texto&gt; para una respuesta pública. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>role=&lt;role&gt; &amp;&amp; mode=&lt;add&#124;remove&#124;toggle&gt;</code> | Cambie la función del miembro que hace clic. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;customdb:delete &lt;namespace&gt; &amp;&amp; &lt;key&gt;&#125;</code> | Eliminar datos personalizados como acción directa. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;customdb:increment &lt;namespace&gt; &amp;&amp; &lt;key&gt; &amp;&amp; [amount]&#125;</code> | Incrementar los datos personalizados como acción directa. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;customdb:set &lt;namespace&gt; &amp;&amp; &lt;key&gt; &amp;&amp; &lt;value&gt;&#125;</code> | Guarde datos personalizados como una acción directa. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;delete_message&#125;</code> | Elimine el mensaje que activó el comando personalizado. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;econ:add &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Agregue moneda de billetera. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;econ:remove &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Elimine la moneda de la billetera sin que el saldo esté por debajo de cero. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;econ:set &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Establecer un saldo de billetera. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;econ:transfer &lt;source&gt; &amp;&amp; &lt;destination&gt; &amp;&amp; &lt;amount&gt;&#125;</code> | Transfiera moneda de billetera sin impuestos. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:ban &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Prohibir a un miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:kick &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Patear a un miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:mute &lt;target&gt; &amp;&amp; &lt;duration&gt; &amp;&amp; [reason]&#125;</code> | Alias ​​de la acción de tiempo de espera. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:role.add &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Agregue un rol manejable a un miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:role.remove &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Eliminar un rol manejable de un miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:role.toggle &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Agregue o elimine un rol según si el miembro lo tiene. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:softban &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Prohibir, eliminar un día de mensajes y luego desbloquear al miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:timeout &lt;target&gt; &amp;&amp; &lt;duration&gt; &amp;&amp; [reason]&#125;</code> | Tiempo de espera de un miembro por hasta 28 días. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:unmute &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Alias ​​de la acción sin tiempo de espera. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:untimeout &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Borrar el tiempo de espera de un miembro. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;mod:warn &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Cree un registro de advertencia. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;run_invoke:&lt;event&gt;&#125;</code> | Ejecute un evento de invocación configurado. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>&#123;run_named_action:&lt;name&gt;&#125;</code> | Ejecute una acción con nombre configurada. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
| <code>$v&#123;select: &lt;id&gt; &amp;&amp; &lt;placeholder&gt; &amp;&amp; &lt;label&gt;&#124;action=&lt;action-spec&gt;&#124;[description]&#124;[emoji]&#125;</code> | Cree una opción desplegable que ejecute una acción. | <code>Reemplazar campos entre corchetes angulares; Los campos entre corchetes son opcionales.</code> | Comandos personalizados; las acciones protegidas requieren permisos y restricciones coincidentes |
