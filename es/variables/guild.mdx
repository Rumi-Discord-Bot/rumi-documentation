---
title: "variables del gremio"
description: "35 variables de gremio apoyadas por Rumi."
icon: "brackets-curly"
---

<Info>
El contexto importa. Una variable regresa `N/A` cuando el comando o evento actual no proporciona su valor fuente. Agregar `|fallback text` antes de la llave de cierre cuando necesita un respaldo más amigable.
</Info>


| Variable | lo que devuelve | Salida de ejemplo | Disponible en |
| --- | --- | --- | --- |
| <code>&#123;guild.afk_channel&#125;</code> | Canal Afk para gremio. | <code>Ejemplo de canal afk del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.afk_timeout&#125;</code> | Afk Tiempo de espera para el gremio. | <code>Ejemplo de tiempo de espera de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.banner&#125;</code> | URL de la imagen del banner. Fuente: gremio. | <code>Ejemplo de banner de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.boost_count&#125;</code> | Recuento de aumento del servidor actual. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.boost_tier&#125;</code> | Nivel de mejora del servidor actual. | <code>Nivel 2</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.category_channels&#125;</code> | Canales de categoría para gremio. | <code>Ejemplos de canales de categoría de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.category_channels_count&#125;</code> | Número de canales de categoría para el gremio. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.channels&#125;</code> | Canales para gremio. | <code>Canales de gremio de ejemplo</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.channels_count&#125;</code> | Número de canales por gremio. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.count&#125;</code> | Recuento de miembros actuales. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.created_at&#125;</code> | Discord-hora de creación formateada. Fuente: gremio. | <code>&lt;t:1781460000:F&gt;</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.created_at_timestamp&#125;</code> | Marca de tiempo Unix para el tiempo de creación. Fuente: gremio. | <code>&lt;t:1781460000:F&gt;</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.discovery&#125;</code> | Descubrimiento para el gremio. | <code>Ejemplo de descubrimiento de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.emoji_count&#125;</code> | Número de emojis personalizados en el servidor. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.icon&#125;</code> | URL de la imagen del icono. Fuente: gremio. | <code>Icono de gremio de ejemplo</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.id&#125;</code> | Discord ID del servidor actual. | <code>123456789012345678</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.key_features&#125;</code> | Características clave para el gremio. | <code>Ejemplo de características clave del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.max_members&#125;</code> | Miembros máximos para el gremio. | <code>Ejemplo de miembros máximos del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.max_presences&#125;</code> | Presencias máximas para el gremio. | <code>Ejemplo de presencia máxima de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.max_video_channel_users&#125;</code> | Usuarios máximos de canales de video para el gremio. | <code>Ejemplo de usuarios del canal de vídeo máximo del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.member_count&#125;</code> | Recuento de miembros actuales. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.name&#125;</code> | Nombre del servidor actual. | <code>Desarrollo Rumi</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.owner_id&#125;</code> | Identificación del propietario del gremio. | <code>Ejemplo de identificación del propietario del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.preferred_locale&#125;</code> | Ubicación preferida para el gremio. | <code>Ejemplo de ubicación preferida del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.region&#125;</code> | Región para gremio. | <code>Ejemplo de región de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.role_count&#125;</code> | Número de roles en el servidor. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.shard&#125;</code> | Fragmento para gremio. | <code>Ejemplo de fragmento de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.splash&#125;</code> | Splash para el gremio. | <code>Ejemplo de bienvenida de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.text_channels&#125;</code> | Canales de texto para gremio. | <code>Ejemplos de canales de texto de gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.text_channels_count&#125;</code> | Número de canales de texto para el gremio. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.vanity&#125;</code> | Vanidad para el gremio. | <code>Ejemplo de vanidad del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.voice_channels&#125;</code> | Canales de voz para gremio. | <code>Ejemplos de canales de voz del gremio</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;guild.voice_channels_count&#125;</code> | Número de canales de voz para el gremio. | <code>128</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;server.id&#125;</code> | Discord IDENTIFICACIÓN. Fuente: servidor. | <code>123456789012345678</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
| <code>&#123;server.name&#125;</code> | Nombre del servidor actual. | <code>Desarrollo Rumi</code> | Incrustaciones, comandos personalizados, respuestas automáticas, mensajes de invocación |
