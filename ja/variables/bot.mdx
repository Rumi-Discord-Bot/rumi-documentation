---
title: "ボット変数"
description: "Rumi がサポートする 31 個のボット変数。"
icon: "brackets-curly"
---

<Info>
コンテキストが重要です。変数が返す `N/A` 現在のコマンドまたはイベントがソース値を提供しない場合。追加 `|fallback text` よりフレンドリーなフォールバックが必要な場合は、右中括弧の前に置きます。
</Info>


| 変数 | 返されるもの | 出力例 | で利用可能 |
| --- | --- | --- | --- |
| <code>&#123;bot.avatar&#125;</code> | アバター画像のURL。出典:ボット。 | <code>https://cdn.discordapp.com/.../avatar.png</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.command_count&#125;</code> | ボットのコマンドの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.custom.avatar&#125;</code> | アバター画像のURL。出典: ボットカスタム。 | <code>https://cdn.discordapp.com/.../avatar.png</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.custom.banner&#125;</code> | バナー画像のURL。出典: ボットカスタム。 | <code>ボットのカスタム バナーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.custom.bio&#125;</code> | ボットカスタムのバイオ。 | <code>ボットのカスタムプロフィールの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.custom.name&#125;</code> | 名前の値。出典: ボットカスタム。 | <code>ルミ</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.dashboard&#125;</code> | ボット用のダッシュボード。 | <code>ボットダッシュボードの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.guild_count&#125;</code> | ボットのギルドの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.id&#125;</code> | Discord ID。出典:ボット。 | <code>123456789012345678</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.invite&#125;</code> | ボットに招待します。 | <code>ボット招待の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.mention&#125;</code> | 言及 Rumi. | <code>&lt;@123456789012345678&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.name&#125;</code> | Rumi ボットのユーザー名。 | <code>ルミ</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.prefix&#125;</code> | 現在のコマンドプレフィックス。 | <code>,</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.color.alert&#125;</code> | ボットの応答色のアラート。 | <code>ボット応答カラーアラートの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.color.bad&#125;</code> | ボットの返信色としては悪いです。 | <code>ボットの返信の色が悪い例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.color.good&#125;</code> | ボットの返信色に適しています。 | <code>例 ボットの応答色が良い</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.color.info&#125;</code> | ボットの返信色の情報。 | <code>ボット応答の色情報の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.color.list&#125;</code> | ボットの返信色のリスト。 | <code>ボット返信カラーリストの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.emoji.alert&#125;</code> | ボット返信絵文字のアラート。 | <code>ボット返信絵文字アラートの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.emoji.bad&#125;</code> | ボットの返信絵文字には適していません。 | <code>ボットの返信絵文字の例が間違っています</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.emoji.good&#125;</code> | ボットの返信絵文字に適しています。 | <code>ボットの返信絵文字の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.emoji.info&#125;</code> | ボット返信絵文字の情報。 | <code>ボットの返信絵文字情報の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.emoji.list&#125;</code> | ボット返信絵文字のリスト。 | <code>ボット返信絵文字リストの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.reply.mode&#125;</code> | 設定されたモード。出典: ボットの返信。 | <code>ボット応答モードの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.shard_count&#125;</code> | ボットのシャードの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.shard_id&#125;</code> | ボットのシャード ID。 | <code>ボットシャード ID の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.support_server&#125;</code> | ボットのサポートサーバー。 | <code>ボットサポートサーバーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.uptime&#125;</code> | このボット プロセスが実行されている時間。 | <code>ボットの稼働時間の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.user_count&#125;</code> | ボットのユーザー数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.version&#125;</code> | ボットのバージョン。 | <code>ボットのバージョンの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;bot.website&#125;</code> | ボット用の Web サイト。 | <code>ボット Web サイトの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
