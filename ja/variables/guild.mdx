---
title: "ギルド変数"
description: "Rumi がサポートする 35 のギルド変数。"
icon: "brackets-curly"
---

<Info>
コンテキストが重要です。変数が返す `N/A` 現在のコマンドまたはイベントがソース値を提供しない場合。追加 `|fallback text` よりフレンドリーなフォールバックが必要な場合は、右中括弧の前に置きます。
</Info>


| 変数 | 返されるもの | 出力例 | で利用可能 |
| --- | --- | --- | --- |
| <code>&#123;guild.afk_channel&#125;</code> | ギルドのAFKチャンネル。 | <code>ギルド afk チャネルの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.afk_timeout&#125;</code> | ギルドのAFKタイムアウト。 | <code>ギルド afk タイムアウトの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.banner&#125;</code> | バナー画像のURL。出典：ギルド。 | <code>ギルドバナーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.boost_count&#125;</code> | 現在のサーバーブースト数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.boost_tier&#125;</code> | 現在のサーバーブースト層。 | <code>レベル2</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.category_channels&#125;</code> | ギルドのカテゴリチャンネル。 | <code>ギルドカテゴリチャンネルの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.category_channels_count&#125;</code> | ギルドのカテゴリチャンネルの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.channels&#125;</code> | ギルドのチャンネル。 | <code>ギルドチャンネルの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.channels_count&#125;</code> | ギルドのチャンネル数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.count&#125;</code> | 現在の会員数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.created_at&#125;</code> | Discord-フォーマットされた作成時間。出典：ギルド。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.created_at_timestamp&#125;</code> | 作成時の Unix タイムスタンプ。出典：ギルド。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.discovery&#125;</code> | ギルドの発見。 | <code>ギルド発見の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.emoji_count&#125;</code> | サーバー内のカスタム絵文字の数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.icon&#125;</code> | アイコン画像のURL。出典：ギルド。 | <code>ギルドアイコンの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.id&#125;</code> | Discord 現在のサーバーのID。 | <code>123456789012345678</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.key_features&#125;</code> | ギルドの主な機能。 | <code>ギルドの主な機能の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.max_members&#125;</code> | ギルドの最大メンバー数。 | <code>ギルド最大メンバーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.max_presences&#125;</code> | ギルドの最大プレゼンス。 | <code>ギルド最大プレゼンスの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.max_video_channel_users&#125;</code> | ギルドの最大ビデオ チャネル ユーザー数。 | <code>ギルド最大ビデオチャンネルユーザーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.member_count&#125;</code> | 現在の会員数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.name&#125;</code> | 現在のサーバー名。 | <code>ルミ開発</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.owner_id&#125;</code> | ギルドのオーナー ID。 | <code>ギルドオーナーIDの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.preferred_locale&#125;</code> | ギルドの優先ロケール。 | <code>ギルドの優先ロケールの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.region&#125;</code> | ギルドの地域。 | <code>ギルド地域の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.role_count&#125;</code> | サーバー内のロールの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.shard&#125;</code> | ギルド用のシャード。 | <code>ギルドシャードの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.splash&#125;</code> | ギルドのスプラッシュ。 | <code>ギルドスプラッシュの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.text_channels&#125;</code> | ギルドのテキストチャンネル。 | <code>ギルドテキストチャネルの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.text_channels_count&#125;</code> | ギルドのテキストチャンネルの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.vanity&#125;</code> | ギルドの虚栄心。 | <code>ギルドバニティの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.voice_channels&#125;</code> | ギルドのボイスチャンネル。 | <code>ギルドボイスチャンネルの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;guild.voice_channels_count&#125;</code> | ギルドの音声チャンネルの数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;server.id&#125;</code> | Discord ID。ソース: サーバー。 | <code>123456789012345678</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;server.name&#125;</code> | 現在のサーバー名。 | <code>ルミ開発</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
