---
title: "ユーザー変数"
description: "Rumi では 30 のユーザー変数がサポートされています。"
icon: "brackets-curly"
---

<Info>
コンテキストが重要です。変数が返す `N/A` 現在のコマンドまたはイベントがソース値を提供しない場合。追加 `|fallback text` よりフレンドリーなフォールバックが必要な場合は、右中括弧の前に置きます。
</Info>


| 変数 | 返されるもの | 出力例 | で利用可能 |
| --- | --- | --- | --- |
| <code>&#123;user&#125;</code> | 応答をトリガーしたユーザーを表示します。 | <code>&lt;@123456789012345678&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.avatar&#125;</code> | トリガーユーザーのアバター URL。 | <code>https://cdn.discordapp.com/.../avatar.png</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.badges&#125;</code> | ユーザー向けのバッジ。 | <code>ユーザーバッジの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.badges_icons&#125;</code> | ユーザーのバッジ アイコン。 | <code>ユーザーバッジアイコンの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.banner&#125;</code> | バナー画像のURL。ソース: ユーザー。 | <code>ユーザーバナーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.boost_count&#125;</code> | ユーザーのブースト数。 | <code>128</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.boost_since&#125;</code> | ユーザーのブースト以降。 | <code>ユーザーブーストの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.boost_since_timestamp&#125;</code> | ブースト以降の Unix タイムスタンプ。 | <code>1781460000</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.color&#125;</code> | ユーザー向けの色。 | <code>ユーザーカラーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.created_at&#125;</code> | Discord- ユーザー アカウントが作成された形式の時刻。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.created_at_timestamp&#125;</code> | 作成時の Unix タイムスタンプ。ソース: ユーザー。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.display_avatar&#125;</code> | ユーザーのアバターを表示します。 | <code>ユーザー表示アバターの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.display_name&#125;</code> | トリガーメンバーのサーバー表示名またはニックネーム。 | <code>月光</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.global_name&#125;</code> | トリガーしたユーザーのグローバル表示名。 | <code>ユーザーのグローバル名の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.guild_avatar&#125;</code> | ユーザーのギルドアバター。 | <code>ユーザーギルドアバターの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.guild_banner&#125;</code> | ユーザー用のギルドバナー。 | <code>ユーザーギルドバナーの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.id&#125;</code> | Discord トリガーしたユーザーの ID。 | <code>123456789012345678</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.is_boost&#125;</code> | ユーザーのブーストです。 | <code>ユーザーの例は boost です</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.is_bot&#125;</code> | ユーザー向けのボットです。 | <code>ユーザーの例はボットです</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.join_position&#125;</code> | ユーザーの結合位置。 | <code>ユーザーの参加位置の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.join_position_sfx&#125;</code> | ユーザーの位置 Sfx を結合します。 | <code>ユーザー参加位置 sfx の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.joined_at&#125;</code> | Discord-メンバーがこのサーバーに参加したときの形式化された時刻。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.joined_at_timestamp&#125;</code> | サーバー参加時間の Unix タイムスタンプ。ソース: ユーザー。 | <code>&lt;t:1781460000:F&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.mention&#125;</code> | メッセージをトリガーしたユーザーについて言及します。 | <code>&lt;@123456789012345678&gt;</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.name&#125;</code> | トリガーしたユーザーのユーザー名。 | <code>ルミ</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.role_list&#125;</code> | ユーザーの役割のリストを、最も高いものから順に記載します。 | <code>ユーザー役割リストの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.role_list_text&#125;</code> | ユーザー ロールのプレーン テキスト リスト (最も高いものから順)。 | <code>ユーザー役割リストのテキストの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.tag&#125;</code> | Discord 利用可能な場合、ユーザー名タグ/識別子スタイルの値。 | <code>ユーザー名</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.top_role&#125;</code> | ユーザーのトップの役割。 | <code>ユーザーのトップロールの例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
| <code>&#123;user.username&#125;</code> | トリガーしたユーザーのユーザー名。 | <code>ユーザーのユーザー名の例</code> | 埋め込み、カスタム コマンド、自動応答、メッセージの呼び出し |
