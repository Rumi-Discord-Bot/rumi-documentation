---
title: "Niveauvariabelen"
description: "15 niveauvariabelen ondersteund door Rumi."
icon: "brackets-curly"
---

<Info>
Context is belangrijk. Een variabele retourneert `N/A` wanneer de huidige opdracht of gebeurtenis de bronwaarde niet levert. Toevoegen `|fallback text` vóór de sluitbeugel als je een vriendelijkere terugval nodig hebt.
</Info>


| Variabel | Wat het teruggeeft | Voorbeelduitvoer | Beschikbaar binnen |
| --- | --- | --- | --- |
| <code>&#123;level&#125;</code> | Niveau- en XP-gegevens voor het lid dat een hoger niveau heeft bereikt. | <code>Voorbeeld niveau</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.new_rank&#125;</code> | Nieuw niveau bereikt door het lid. | <code>12</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.next_rank&#125;</code> | Volgende niveau na het huidige. | <code>Voorbeeldniveau volgende rang</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.old_rank&#125;</code> | Vorig niveau vóór het niveau omhoog. | <code>11</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.progress&#125;</code> | Vooruitgang naar het volgende niveau. | <code>74%</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.rank_position&#125;</code> | Positie in ranglijst voor het lid. | <code>Voorbeeld niveau rangpositie</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.server_xp&#125;</code> | Server Xp voor niveau. | <code>Voorbeeld niveau server xp</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.server_xp_total&#125;</code> | Server Xp Totaal voor niveau. | <code>Voorbeeld niveau server xp totaal</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.total_messages&#125;</code> | Totaal aantal berichten voor niveau. | <code>Totaal aantal berichten op voorbeeldniveau</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.user_xp&#125;</code> | Huidige XP-waarde voor het lid. | <code>8,420</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.user_xp_total&#125;</code> | Gebruiker Xp Totaal voor niveau. | <code>Voorbeeldniveau gebruiker xp totaal</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.voice_seconds&#125;</code> | Stemseconden voor niveau. | <code>Voorbeeldniveau stemseconden</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.voice_time&#125;</code> | Stemtijd voor niveau. | <code>Voorbeeld niveau stemtijd</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;level.xp_needed&#125;</code> | XP nodig voor het volgende niveau. | <code>9,000</code> | Niveauberichten, niveauvoorbeelden |
| <code>&#123;xp&#125;</code> | Niveau- en XP-gegevens voor het lid dat een hoger niveau heeft bereikt. | <code>Voorbeeld xp</code> | Niveauberichten, niveauvoorbeelden |
