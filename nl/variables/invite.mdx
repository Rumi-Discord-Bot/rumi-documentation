---
title: "Nodig variabelen uit"
description: "11 uitnodigingsvariabelen ondersteund door Rumi."
icon: "brackets-curly"
---

<Info>
Context is belangrijk. Een variabele retourneert `N/A` wanneer de huidige opdracht of gebeurtenis de bronwaarde niet levert. Toevoegen `|fallback text` vóór de sluitbeugel als je een vriendelijkere terugval nodig hebt.
</Info>


| Variabel | Wat het teruggeeft | Voorbeelduitvoer | Beschikbaar binnen |
| --- | --- | --- | --- |
| <code>&#123;invite.channel&#125;</code> | Kanaalvermelding of kanaalwaarde. Bron: uitnodiging. | <code>Voorbeeld uitnodigingskanaal</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.channel_id&#125;</code> | Discord kanaal-ID. Bron: uitnodiging. | <code>Voorbeeld uitnodigingskanaal-ID</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.code&#125;</code> | Code voor uitnodiging. | <code>Voorbeeld uitnodigingscode</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.created_at&#125;</code> | Discord-geformatteerde aanmaaktijd. Bron: uitnodiging. | <code>&lt;t:1781460000:F&gt;</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.created_at_timestamp&#125;</code> | Unix-tijdstempel voor aanmaaktijd. Bron: uitnodiging. | <code>&lt;t:1781460000:F&gt;</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.inviter&#125;</code> | Uitnodiger voor uitnodiging. | <code>Voorbeeld uitnodigingsuitnodiger</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.inviter_id&#125;</code> | Uitnodigings-ID voor uitnodiging. | <code>Voorbeeld van uitnodigings-ID</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.inviter_mention&#125;</code> | Uitnodiger Vermelding voor uitnodiging. | <code>Voorbeeld van vermelding van uitnodigingsuitnodiger</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.max_uses&#125;</code> | Max. gebruik voor uitnodiging. | <code>Voorbeeld van maximaal gebruik van uitnodiging</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.url&#125;</code> | Directe URL. Bron: uitnodiging. | <code>https://discord.com/channels/123/456/789</code> | Nodig trackingberichten en logs uit |
| <code>&#123;invite.uses&#125;</code> | Gebruik voor uitnodiging. | <code>Voorbeeld van uitnodigingsgebruik</code> | Nodig trackingberichten en logs uit |
