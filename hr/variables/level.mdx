---
title: "Varijable razine"
description: "15 varijabli razine koje podržava Rumi."
icon: "brackets-curly"
---

<Info>
Kontekst je bitan. Vraća se varijabla `N/A` kada trenutna naredba ili događaj ne daje svoju izvornu vrijednost. Dodati `|fallback text` prije završne vitičaste zagrade kada trebate prijateljskiju zamjenu.
</Info>


| Varijabilna | Što se vraća | Primjer izlaza | Dostupno u |
| --- | --- | --- | --- |
| <code>&#123;level&#125;</code> | Podaci o razini i XP-u za člana koji je prešao na višu razinu. | <code>Primjer razine</code> | Poruke razine, pregledi razine |
| <code>&#123;level.new_rank&#125;</code> | Član je dosegao novu razinu. | <code>12</code> | Poruke razine, pregledi razine |
| <code>&#123;level.next_rank&#125;</code> | Sljedeća razina nakon trenutne. | <code>Primjer sljedeće razine</code> | Poruke razine, pregledi razine |
| <code>&#123;level.old_rank&#125;</code> | Prethodna razina prije razine više. | <code>11</code> | Poruke razine, pregledi razine |
| <code>&#123;level.progress&#125;</code> | Napredak prema sljedećoj razini. | <code>74%</code> | Poruke razine, pregledi razine |
| <code>&#123;level.rank_position&#125;</code> | Položaj na ljestvici najboljih za člana. | <code>Primjer rang pozicije razine</code> | Poruke razine, pregledi razine |
| <code>&#123;level.server_xp&#125;</code> | Server Xp za razinu. | <code>Primjer poslužitelja razine xp</code> | Poruke razine, pregledi razine |
| <code>&#123;level.server_xp_total&#125;</code> | Server Xp Ukupno za razinu. | <code>Primjer razine server xp ukupno</code> | Poruke razine, pregledi razine |
| <code>&#123;level.total_messages&#125;</code> | Ukupno poruka za razinu. | <code>Primjer razine ukupnog broja poruka</code> | Poruke razine, pregledi razine |
| <code>&#123;level.user_xp&#125;</code> | Trenutna XP vrijednost za člana. | <code>8,420</code> | Poruke razine, pregledi razine |
| <code>&#123;level.user_xp_total&#125;</code> | Ukupno XP korisnika za razinu. | <code>Primjer ukupnog broja xp korisnika razine</code> | Poruke razine, pregledi razine |
| <code>&#123;level.voice_seconds&#125;</code> | Glas Sekunde za razinu. | <code>Primjer sekunde glasovne razine</code> | Poruke razine, pregledi razine |
| <code>&#123;level.voice_time&#125;</code> | Glas Vrijeme za razinu. | <code>Glasovno vrijeme razine primjera</code> | Poruke razine, pregledi razine |
| <code>&#123;level.xp_needed&#125;</code> | XP potreban za sljedeću razinu. | <code>9,000</code> | Poruke razine, pregledi razine |
| <code>&#123;xp&#125;</code> | Podaci o razini i XP-u za člana koji je prešao na višu razinu. | <code>Primjer xp</code> | Poruke razine, pregledi razine |
