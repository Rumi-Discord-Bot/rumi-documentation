---
title: "Opće varijable"
description: "9 općih varijabli koje podržava Rumi."
icon: "brackets-curly"
---

<Info>
Kontekst je bitan. Vraća se varijabla `N/A` kada trenutna naredba ili događaj ne daje svoju izvornu vrijednost. Dodati `|fallback text` prije završne vitičaste zagrade kada trebate prijateljskiju zamjenu.
</Info>


| Varijabilna | Što se vraća | Primjer izlaza | Dostupno u |
| --- | --- | --- | --- |
| <code>&#123;len:value&#125;</code> | Vrati broj znakova. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;length:value&#125;</code> | Alias ​​of &#123;len:vrijednost&#125;. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;lower:value&#125;</code> | Pretvorite razriješenu vrijednost u mala slova. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;title:value&#125;</code> | Pretvorite razriješenu vrijednost u velika i mala slova. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;upper:value&#125;</code> | Pretvorite razriješenu vrijednost u velika slova. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;urlencode:value&#125;</code> | Kodirajte razriješenu vrijednost za upotrebu u URL-u. | <code>Pogledajte sintaksu i primjere u vodičima za prilagođene naredbe.</code> | Prilagođene naredbe; pomoćnici prilagođenih podataka također zahtijevaju --db u automatskim odgovorima |
| <code>&#123;newline&#125;</code> | Umetnite pravi prijelom retka. | <code>\n</code> | Ugradnje, prilagođene naredbe, automatski odgovori, dozivanje poruka |
| <code>&#123;prefix&#125;</code> | Trenutačni prefiks poslužitelja. | <code>,</code> | Ugradnje, prilagođene naredbe, automatski odgovori, dozivanje poruka |
| <code>&#123;timestamp&#125;</code> | Kontekst vremenske oznake. | <code>1781460000</code> | Ugradnje, prilagođene naredbe, automatski odgovori, dozivanje poruka |
