---
title: "Naredbene varijable"
description: "11 naredbenih varijabli koje podržava Rumi."
icon: "brackets-curly"
---

<Info>
Kontekst je bitan. Vraća se varijabla `N/A` kada trenutna naredba ili događaj ne daje svoju izvornu vrijednost. Dodati `|fallback text` prije završne vitičaste zagrade kada trebate prijateljskiju zamjenu.
</Info>


| Varijabilna | Što se vraća | Primjer izlaza | Dostupno u |
| --- | --- | --- | --- |
| <code>&#123;command.alias&#125;</code> | Alias ​​koji se koristi za pokretanje naredbe kada je dostupan. | <code>Primjer pseudonima naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.arg_count&#125;</code> | Broj argumenata za naredbu. | <code>128</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.category&#125;</code> | Kategorija za naredbu. | <code>Primjer kategorije naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.channel&#125;</code> | Spominjanje kanala ili vrijednost kanala. Izvor: naredba. | <code>Primjer naredbenog kanala</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.channel_id&#125;</code> | Discord ID kanala. Izvor: naredba. | <code>Primjer ID-a naredbenog kanala</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.description&#125;</code> | Tekst opisa. Izvor: naredba. | <code>Primjer opisa naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.name&#125;</code> | Naziv naredbe koja trenutno prikazuje ovaj odgovor. | <code>Rumi</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.usage&#125;</code> | Upotreba za naredbu. | <code>Primjer korištenja naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.user&#125;</code> | Korisnik za naredbu. | <code>Primjer korisnika naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.user_id&#125;</code> | Korisnički ID za naredbu. | <code>Primjer ID-a korisnika naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
| <code>&#123;command.user_mention&#125;</code> | Spominjanje korisnika za naredbu. | <code>Primjer spomena korisnika naredbe</code> | Prilagođene naredbe i predlošci svjesni naredbi |
