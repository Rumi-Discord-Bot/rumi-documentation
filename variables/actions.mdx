---
title: "Actions variables"
description: "30 actions variables supported by Rumi."
icon: "brackets-curly"
---

<Info>
These rows are syntax templates, not copy-ready examples. Replace every `<required>` field, omit or replace `[optional]` fields, and keep direct-action arguments in the displayed order. Keyed button fields such as `label=`, `style=`, and `action=` may be rearranged.
</Info>

For complete argument rules, component chaining, dropdown layout, and permission checks, read [Custom-command actions](/custom-commands/actions).


| Exact syntax | What it does | How to read it | Available in |
| --- | --- | --- | --- |
| <code>$v&#123;button: label=&lt;text&gt; &amp;&amp; style=&lt;style&gt; &amp;&amp; action=&lt;action-spec&gt; &amp;&amp; [reply=&lt;text&gt;]&#125;</code> | Create a button that runs an action. Keyed fields may be rearranged. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>$v&#123;button: label=&lt;text&gt; &amp;&amp; url=&lt;https-url&gt; &amp;&amp; [emoji=&lt;emoji&gt;]&#125;</code> | Create a link button. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>allow_user=&lt;id&gt; &amp;&amp; allow_role=&lt;id&gt; &amp;&amp; allow_channel=&lt;id&gt;</code> | Restrict who or where a component can run. Each field also accepts comma-separated IDs. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>action=&lt;action-spec&gt; &amp;&amp; action=&lt;action-spec&gt;</code> | Run component actions from left to right. Repeat action= for each step. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>await=true &amp;&amp; once=true &amp;&amp; defer=true &amp;&amp; expires=&lt;duration&gt;</code> | Control repeat execution, deferral, and expiry. These keyed fields may be rearranged. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>page=&lt;next&#124;previous&#124;number&gt;</code> | Move an interactive response to another $v&#123;page&#125;. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>reply=&lt;text&gt;</code> | Send an ephemeral component response. Use public=&lt;text&gt; for a public response. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>role=&lt;role&gt; &amp;&amp; mode=&lt;add&#124;remove&#124;toggle&gt;</code> | Change the clicking member role. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;customdb:delete &lt;namespace&gt; &amp;&amp; &lt;key&gt;&#125;</code> | Delete custom data as a direct action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;customdb:increment &lt;namespace&gt; &amp;&amp; &lt;key&gt; &amp;&amp; [amount]&#125;</code> | Increase custom data as a direct action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;customdb:set &lt;namespace&gt; &amp;&amp; &lt;key&gt; &amp;&amp; &lt;value&gt;&#125;</code> | Save custom data as a direct action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;delete_message&#125;</code> | Delete the message that triggered the custom command. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;econ:add &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Add wallet currency. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;econ:remove &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Remove wallet currency without taking the balance below zero. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;econ:set &lt;target&gt; &amp;&amp; &lt;amount&gt; &amp;&amp; [reason]&#125;</code> | Set a wallet balance. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;econ:transfer &lt;source&gt; &amp;&amp; &lt;destination&gt; &amp;&amp; &lt;amount&gt;&#125;</code> | Transfer wallet currency without tax. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:ban &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Ban a member. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:kick &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Kick a member. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:mute &lt;target&gt; &amp;&amp; &lt;duration&gt; &amp;&amp; [reason]&#125;</code> | Alias of the timeout action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:role.add &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Add a manageable role to a member. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:role.remove &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Remove a manageable role from a member. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:role.toggle &lt;target&gt; &amp;&amp; &lt;role&gt; &amp;&amp; [reason]&#125;</code> | Add or remove a role based on whether the member has it. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:softban &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Ban, remove one day of messages, then unban the member. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:timeout &lt;target&gt; &amp;&amp; &lt;duration&gt; &amp;&amp; [reason]&#125;</code> | Timeout a member for up to 28 days. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:unmute &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Alias of the untimeout action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:untimeout &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Clear a member timeout. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;mod:warn &lt;target&gt; &amp;&amp; [reason]&#125;</code> | Create a warning record. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;run_invoke:&lt;event&gt;&#125;</code> | Run a configured invoke event. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>&#123;run_named_action:&lt;name&gt;&#125;</code> | Run a configured named action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
| <code>$v&#123;select: &lt;id&gt; &amp;&amp; &lt;placeholder&gt; &amp;&amp; &lt;label&gt;&#124;action=&lt;action-spec&gt;&#124;[description]&#124;[emoji]&#125;</code> | Create a dropdown option that runs an action. | <code>Replace angle-bracket fields; square-bracket fields are optional.</code> | Custom commands; protected actions require matching permissions and restrictions |
